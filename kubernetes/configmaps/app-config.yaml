apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ENVIRONMENT: "development"  # Example environment variable
  # Add other non-sensitive app-specific configuration variables here. bro wifi notnher ? 

